import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export default function PageSelectionScreen({navigation}: any){
    // Displau every list of passages that are available inside the json file (ReadingMaterials).

    return(
        <SafeAreaView>
            
        </SafeAreaView>
    );
}