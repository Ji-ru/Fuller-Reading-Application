import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";


export default function SignUpTwoScreen({navigation}: any) {
    // Add navigation code here for SignUpCompletedScreen
    return(
        <SafeAreaView>
            
        </SafeAreaView>
    );
}