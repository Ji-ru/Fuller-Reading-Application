import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ReadingActivityScreen({navigation}: any) {

    return (
        <SafeAreaView>

            
        </SafeAreaView>
    );
}