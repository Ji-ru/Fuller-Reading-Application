import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignUpOneScreen({navigation}: any) {
    // Add navigation code for SignUpTwoScreen

    return (
        <SafeAreaView>

            
        </SafeAreaView>
    );
}